def isprime(n):
	for x in range(2,n):
		if n%x==0:
			return False
	return True

def rotate(n):
	nstr = str(n)
	nstr = nstr[len(nstr)-1] + nstr[0:len(nstr)-1]
	return int(nstr)
ans = 1
for num in range(3,1000000):
	if isprime(num):
		possible = len(str(num))
		count = 1
		test = num
		print num
		if possible > 1:			for x in range(0,possible1):
				next = rotate(test)
				print next
				if not isprime(next):
					break
				else:
					count += 1
					test = next
		if count == possible:
			ans += 1
			print num
print ans
